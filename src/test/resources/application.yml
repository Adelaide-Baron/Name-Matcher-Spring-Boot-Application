
spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/mini_project?sslMode=PREFERRED&tlsVersions=TLSv1.2}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
